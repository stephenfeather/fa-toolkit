<?xml version="1.0"?>
<ruleset name="FA Toolkit - Strict WPCS">
	<description>Strict WordPress Coding Standards with pragmatic loader/CLI handling.</description>

	<!-- CLI args -->
	<arg name="extensions" value="php"/>
	<arg name="colors"/>
	<arg value="ps"/>

	<!-- PHP compatibility -->
	<config name="testVersion" value="8.3"/>

	<!-- What to scan -->
	<file>src</file>
	<file>includes</file>

	<!-- Ignore vendor/build artifacts -->
	<exclude-pattern>vendor/*</exclude-pattern>
	<exclude-pattern>node_modules/*</exclude-pattern>
	<exclude-pattern>dist/*</exclude-pattern>
	<exclude-pattern>build/*</exclude-pattern>

	<!-- Standards -->
	<rule ref="WordPress-Core"/>
	<rule ref="WordPress-Extra"/>
	<rule ref="WordPress-Docs"/>

	<!-- Enforce: output must be escaped; input must be validated/sanitized -->
	<rule ref="WordPress.Security.EscapeOutput"/>
	<rule ref="WordPress.Security.ValidatedSanitizedInput"/>

	<!-- Strong stance: forbid $_REQUEST usage (force GET or POST) -->
	<rule ref="WordPress.Security.SuperglobalInputUsage">
		<properties>
			<property name="allowed_superglobals" value="_GET,_POST"/>
		</properties>
	</rule>

	<!-- Allow direct-access guards in leaf files (still strict elsewhere) -->
	<rule ref="WordPress.PHP.DiscouragedPHPFunctions.exit">
		<exclude-pattern>*/includes/*.php</exclude-pattern>
		<exclude-pattern>*/src/*.php</exclude-pattern>
	</rule>

	<!-- CLI code may legitimately output raw CLI text; keep strict in web paths -->
	<rule ref="WordPress.Security.EscapeOutput.OutputNotEscaped">
		<exclude-pattern>includes/cli/*</exclude-pattern>
	</rule>
</ruleset>
